<div class="form-inline">
	{{#if perPageOptions}}
	<select name="perPage" style="width: auto; margin-left: 10px;"></select>
	{{/if}}
	{{#if orderByOptions}}
	<select name="orderBy" style="width: auto;"></select>
	{{/if}}
</div>